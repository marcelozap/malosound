# MaloSound daily market publishing

The user authorized public posting of the market research, interactive map, closing chart and song selection to https://malosound.ai in this task. This replaces the private-only destination in the older Market Orbit guides. The current schedule is Monday–Friday at 8:30 a.m. and 4:00 p.m. America/New_York, with clearly labeled market-closed notes on exchange holidays. The current Windows runbook is docs/DAILY_PUBLISHING.md; it supersedes older timing and platform-specific source instructions. Generation starts at those times; publication follows research and validation.

The current editorial requirements are in `docs/EDITORIAL_PLAYBOOK.md`. Prepare a private research packet, then publish a concise visual entry through `docs/DAILY_PUBLISHING.md`. Historical Mac source records at /Users/a14/Documents/xiv/market-journal are legacy reference; that path is not the Windows run destination. Preserve the original source report and all prior dates; do not invent a morning forecast when none exists. An after-close founding report must remain labeled after-close. A retrospective trial must show both session reference and actual preparation time.

On this Windows setup, use the morning and closing procedures in `docs/DAILY_PUBLISHING.md`. `tools/sync_market_journal.py` is a legacy Mac importer with a platform-specific source path; do not run it as the default scheduled Windows step. Preserve existing original-song entries and unrelated studio work. If new research conflicts with a published edition, save a dated correction and preserve its original content and recording.

Existing listening selections use `song` with title, artist, verified HTTPS URL and original rationale. Label them existing recordings; they are not original MaloSound compositions and are outside the fixed-duration original-song series. No audio download or lyrics. Original recorded releases retain `audioUrl` and `durationSeconds` and must match `songDurationSeconds`; the original series duration is now 195 seconds, established by the September 4 SPY instrumental. Never invent a recording.

Run `python3 tools/build_website.py`, Node syntax checks for journal.js and market-map.js, and relevant content checks. Validate evidence, timestamps, local report/chart links and the generated chart. Stage only this edition's public files and intended website/workflow changes. Never add unrelated drafts, audio, keys, personal data or private studio records. Commit and push to the existing origin/master, which deploys the public malosound.ai site on Vercel. Verify the deployment status and published edition JSON and assets at https://malosound.ai/. Do not change DNS, access settings or the hosting provider. The .openai Sites project is a separate private preview and does not publish the custom domain.

Notify the user with the MaloSound journal link and key changes; closing includes a chart link and listening selection. No social-network posts, messages to other people, trades or other destinations are authorized by this workflow. Do not duplicate a completed edition; if publication failed, retry that exact edition's publication.

Presentation preference: maintain the futuristic, artistic, visual-first homepage. Publish long analysis in the existing collapsed notes and full research pages. Do not add explanatory paragraphs to the hero or expand every source on the homepage. Keep actual session dates, retrospective labels, chart evidence and original/reference-track distinctions visible where relevant.

## Calendar publication format

Follow `DAILY_PUBLISHING.md` for the current calendar and three-part day entry. The archive begins September 3, 2026, and retains the black/gold/electric-blue guitar theme. Each date groups its own before-open note, plain SPY line and original 3:15 instrumental. Preserve source timestamps, historical reconstructions, corrections, and explicit gaps. The build generates the plain line and standalone entry from the preserved public data.

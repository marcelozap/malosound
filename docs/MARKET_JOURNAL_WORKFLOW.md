# MaloSound daily market publishing

The user authorized public posting of the market research, interactive map, closing chart and song selection to https://malosound.ai in this task. This replaces the private-only destination in the older Market Orbit guides. The daily schedule remains 8:30 a.m. and 5:00 p.m. America/New_York, including clearly labeled weekend/holiday recaps. Generation starts at those times; publication follows research and validation.

Research and append the source records at /Users/a14/Documents/xiv/market-journal, using DAILY.md for morning and CLOSING.md for closing analysis, source dates, audit data, and immutable history. Their older private-only publishing instruction is superseded by this explicit user authorization. Preserve the original source report and all prior dates; do not invent a morning forecast when none exists. An after-close founding report must remain labeled after-close. A retrospective trial must show both session reference and actual preparation time.

In this MaloSound checkout run `python3 tools/sync_market_journal.py`. It imports new source editions into content/editions.json, stages public reports/chart assets and updates content/market-map.json. It rejects conflicting changes to an already imported edition; review discrepancies instead of overwriting history. Preserve existing original-song entries and unrelated studio work. If adding a new source edition conflicts with an existing original-song entry, integrate the research fields without replacing its recording or changing series duration.

Existing listening selections use `song` with title, artist, verified HTTPS URL and original rationale. Label them existing recordings; they are not original MaloSound compositions and are outside the fixed-duration original-song series. No audio download or lyrics. Original recorded releases retain `audioUrl` and `durationSeconds` and must match `songDurationSeconds`; do not select a series duration or invent an original recording.

Run `python3 tools/build_website.py`, Node syntax checks for journal.js and market-map.js, and relevant content checks. Validate evidence, timestamps, local report/chart links and the generated chart. Stage only this edition's public files and intended website/workflow changes. Never add unrelated drafts, audio, keys, personal data or private studio records. Commit and push to the existing origin/master, which deploys the public malosound.ai site on Vercel. Verify the deployment status and published edition JSON and assets at https://malosound.ai/. Do not change DNS, access settings or the hosting provider. The .openai Sites project is a separate private preview and does not publish the custom domain.

Notify the user with the MaloSound journal link and key changes; closing includes a chart link and listening selection. No social-network posts, messages to other people, trades or other destinations are authorized by this workflow. Do not duplicate a completed edition; if publication failed, retry that exact edition's publication.
